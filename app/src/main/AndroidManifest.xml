<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="arunreddy.com.travelguide">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TravelGuide">
        <activity android:name=".detailview"></activity>
        <activity android:name=".amusementparks_list" />
        <activity android:name=".movietheater_list" />
        <activity android:name=".museum_list" />
        <activity android:name=".beach_list" />
        <activity android:name=".temple_list" />
        <activity android:name=".hotel_list" />
        <activity android:name=".signup" />
        <activity android:name=".MainActivity" />
        <activity android:name=".login" />
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>